apiVersion: v1
kind: Service
metadata:
  name: ontology-fuseki
  namespace: gis-agent
  labels:
    app.kubernetes.io/name: ontology-fuseki
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ontology-fuseki
  ports:
    - name: http
      port: 3030
      targetPort: http
