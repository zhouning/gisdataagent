# Offline wheelhouse manifest for the built-in Windows GIS path.
# Keep these pins synchronized with the project release requirements.txt and
# build the wheels on a connected staging machine before transfer to the site.
geopandas==1.1.2
pyogrio==0.12.1
rasterio==1.5.0
shapely==2.1.1
pyarrow==20.0.0
pyproj==3.7.2
