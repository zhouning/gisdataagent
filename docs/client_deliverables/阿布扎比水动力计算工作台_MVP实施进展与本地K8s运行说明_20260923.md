# 阿布扎比水动力计算工作台 MVP 实施进展与本地 K8s 运行说明

**日期：** 2026-09-23  
**开发分支：** `feat/abu-dhabi-flood-k8s-workbench-mvp`  
**开发测试环境：** Docker Desktop Kubernetes，独立命名空间 `gis-agent-hydro-dev`

**主应用联调状态：** 已将包含正式水动力工作台 Tab 的 `gis-data-agent:dev` 镜像加载到 Docker Desktop Kubernetes，并完成 `gis-agent` 命名空间滚动发布。主应用可通过 `http://localhost:18080` 访问（端口转发命令见下文）。

## 1. 本次已完成

目前已经形成可以在本机 Kubernetes 中运行的最小闭环：

```text
Web 页面/API
→ 参数与 AOI 校验
→ 冻结不可变 Run Manifest
→ Kubernetes API 动态创建 Job
→ 运行 EPA SWMM、ANUGA 或 SWMM–ANUGA 耦合任务
→ 结果写入持久卷
→ Web API 查询状态、日志、结果和地图 GeoJSON
```

已实现的主要能力：

| 能力 | 当前状态 | 说明 |
|---|---|---|
| 独立 Web 工作台 | 已完成 MVP | 支持绘制矩形 AOI、选择模型和设置基础参数 |
| Run Manifest | 已完成 | 记录 AOI、实际计算域、显示范围、数据来源、参数值、默认值来源和资源档位 |
| K8s Job 动态编排 | 已完成 | API 使用命名空间内 ServiceAccount 创建、查询和取消 Job |
| 一维计算 | 已验证 | EPA SWMM 5.2.4 在独立 Job 中真实运行并生成 INP、RPT、OUT 和地图摘要 |
| 二维计算 | 已验证 | ANUGA 在独立 Job 中真实运行并生成 SWW、深度摘要和 GeoJSON |
| 一二维组合 | 已验证 | SWMM–ANUGA 双向耦合开发 Pilot 已在独立 Job 中运行成功 |
| 状态和结果持久化 | 已完成 MVP | Run Manifest、状态和结果保存在 `hydro-runs` PVC |
| CPU/GPU 资源档位 | 已实现清单 | 本地默认 CPU；GPU 档位在无 GPU 环境中主动拒绝 |
| 日志 API | 已实现 | 读取对应 Job Pod 的最近日志；RBAC 仅授予必要读取权限 |
| 现有 GIS Data Agent API 接入 | 已完成 | 已增加正式导航 Tab、工作台组件、预检接口和三语言文案 |
| 参数预检 | 已完成 MVP | 预检不创建 Job；检查 AOI、参数、必需数据源和客户 ETL 状态 |
| 客户数据防误运行 | 已完成 | `customer_mount` 在 ETL 产出模型原生输入前始终返回 `blocked` |

## 2. 本地验证记录

本次已经在 Docker Desktop Kubernetes 中验证三条真实执行路径：

| 模型路径 | Run ID | K8s 结果 | 关键结果 |
|---|---|---|---|
| 一维 EPA SWMM | `hydro-fab4bab9bb674e81` | Job Complete | SWMM 5.2.4，生成 RPT/OUT；最大节点水深和管段流量已解析 |
| 二维 ANUGA | `hydro-35bfb02751624fc6` | Job Complete | 3,132 个三角形；生成最大水深摘要和深度点 GeoJSON |
| SWMM–ANUGA 双向耦合 | `hydro-3375c29e1ba44a19` | Job Complete | 生成耦合 receipt、delivery summary 和最大水深 GeoJSON |
| SWMM–ANUGA 双向耦合（二次验收） | `hydro-15e9c9889d4d4a33` | Job Complete | 结果回执和 3 个 GeoJSON 要素可通过 API 读取 |

上述运行使用的是开发夹具，用于证明模型运行时、编排、存储和结果查询链路真实可执行。结果被明确标记为：

```text
engineering_use = false
```

不能把这些结果解释为已经完成客户数据 ETL、模型校准或工程预测。

## 3. 当前开发访问方式

启动端口转发：

```bash
./scripts/k8s-hydro-workbench-dev.sh forward
```

访问：

```text
http://localhost:8091
```

上述地址是独立 hydro-api 工作台的访问地址。正式 GIS Data Agent 主应用的联调地址为：

```bash
kubectl -n gis-agent port-forward service/gis-agent-app 18080:80
```

```text
http://localhost:18080
```

已验证主应用的 `/health` 和 `/ready` 返回正常，前端生产资源中包含中文、英文和阿拉伯文的水动力工作台文案。主应用迁移 Job 已成功执行，新的应用 Pod 已完成滚动发布。

正式 GIS Data Agent 中新增的导航项为：

```text
分析与模型 → 世界模型 → 阿布扎比 · 水动力模型计算工作台
```

该 Tab 提供 1D SWMM、2D ANUGA 和 1D+2D coupled 三种选择，支持 AOI bbox、降雨量、历时、降雨过程、二维网格、曼宁糙率、时间步长、一维汇流/下渗方法、耦合方式、资源档位及网络/地形/降雨/潮位/出水口/泵站数据 URI。提交按钮只有在预检返回 `can_submit=true` 时启用。

常用运维命令：

```bash
./scripts/k8s-hydro-workbench-dev.sh build
./scripts/k8s-hydro-workbench-dev.sh deploy
./scripts/k8s-hydro-workbench-dev.sh status
```

部署只使用 `gis-agent-hydro-dev`，不会覆盖已有 `gis-agent` 命名空间。

Docker Desktop 使用 ARM64 水动力运行时。正式 H100 节点应构建同一工作台镜像的
AMD64 版本，例如将 `HYDRO_RUNTIME_IMAGE` 设置为客户环境验证过的 Linux AMD64
运行时，并在镜像仓库中以不可变 digest 发布；不能把本机 ARM64 镜像直接当作 H100
节点镜像。

## 4. 当前数据边界

客户原始 GDB、0.5 m DTM、泵站、潮位和管网监测数据目前不能直接提交给模型。系统当前对 `customer_mount` 执行采取 fail-closed 策略：即使 URI 已填写，在 GDB/DTM ETL 适配器完成之前，也不会静默改用开发夹具计算。

下一步客户数据链应为：

```text
NAS/对象存储原始数据
→ 数据资产登记和版本选择
→ AOI 窗口读取
→ GDB 字段映射、拓扑修复和单位统一
→ DTM COG/窗口裁剪、清洗和重采样
→ 生成模型可用 SWMM INP 与 ANUGA 网格
→ 数据预检
→ 才允许创建模型 Job
```

这是当前最重要的未完成项，也是从“运行演示”进入“客户数据可用”的关键路径。

## 5. 与原架构文档验收项的对应关系

| 验收项 | 当前状态 |
|---|---|
| 无需 SSH，通过网页完成计算 | 已完成本地 MVP |
| 地图区域选择 | 已完成矩形 AOI 开发版；工作台支持 bbox 参数输入 |
| 识别区域内 DTM、管网和设施覆盖 | 未完成，依赖数据目录与 ETL |
| 选择客户数据版本 | 合同已预留，实际执行暂时关闭 |
| 设置降雨量和降雨历时 | 已完成；并显示降雨过程、二维时间步长等参数 |
| 不可变 Run Manifest | 已完成 |
| 动态创建和释放计算容器 | 已完成；Job 完成后按 TTL 回收 |
| 一维结果展示 | 已完成摘要和 GeoJSON MVP |
| 二维结果展示 | 已完成深度点/最大水深 GeoJSON MVP |
| 时间滑块和完整时序图 | 未完成 |
| 参数来源和默认值说明 | 已进入 Run Manifest，并在预检回执中返回 manifest preview |
| 预检后再提交 Job | 已完成；客户数据 ETL 未完成时 fail-closed |
| 网页显示失败原因 | 已完成 API 状态；页面仍可增强 |
| 容器销毁后结果可追溯 | 已完成 PVC MVP；正式环境应接对象存储/NAS |

## 6. 下一批开发任务

1. 建立客户数据资产目录，接入对象存储/NAS URI、版本和权限；
2. 实现 GDB → 规范化管网 → SWMM INP 的区域 ETL Job；
3. 实现 0.5 m DTM → COG 窗口 → 区域 ANUGA 网格的 ETL Job；
4. 实现一维上游/下游水力相关范围抽取，不只做几何裁剪；
5. 在真实 GIS Data Agent 部署中发布已完成的水动力计算工作台 Tab（本地 Docker Desktop 联调已完成，客户环境仍需按其镜像仓库和 K8s 策略发布）；
6. 将结果发布为适合 Web 的时序、COG/MVT/PMTiles，并增加时间滑块；
7. 在客户 K8s 上确认 CSI、对象存储、Ingress、CPU/内存/临时盘和 GPU Operator；
8. 用一个真实小区域和经过 ETL 的客户数据做第一轮端到端验收。

## 7. 关键结论

当前已经证明“网页/API → Manifest → K8s Job → 一维/二维/耦合求解器 → 持久化结果 → Web 查询”的执行链可行，正式 GIS Data Agent Tab 的代码接入和 Docker Desktop 主应用联调也已完成。下一阶段不应继续扩大开发夹具，而应优先完成客户 GDB 和 DTM 的区域 ETL，并在客户主 GIS Data Agent 镜像中按 AMD64/H100 约束发布该 Tab。
